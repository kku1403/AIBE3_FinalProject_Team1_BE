{
  "name": "profile-image-resizer",
  "version": "1.0.0",
  "description": "Lambda function for resizing profile images",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:profile && npm run build:post",
    "build:profile": "node create-zip.js profile",
    "build:post": "node create-zip.js post"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.947.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
